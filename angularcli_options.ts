ANGULAR CLI

Reqs:
- node


ng new nameapp					create new app
ng g name 						generate.. component/service/class/etc..
ng lint							view problems 
ng lint --fix					solve problems

Options blueprints
--dry-run						report the files, don'r write them. well to test
--routing						add a default routing module
--prefix name 					add dif prefix in app
--skip-test						don't generate spec files
--flat							don't create a folder
--inline-template				will the template be in the .ts file?
--inline-style					will the style be in the .ts file?
--spec-false					generate spec?
--view-encapsulation			view encapsulation strategy
--change-detection				change detection strategy


ng g directive					gen. directive
ng g service					gen. service
ng g pipe						gen. pipe
ng g class						gen. class
ng g interface					gen. interface
ng g enum						gen. enum
ng g module						gen. module
